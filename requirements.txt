# Streamlit Community Cloud installs from this file (dashboard only — the
# watcher installs the full package from pyproject.toml in GitHub Actions).
streamlit>=1.40
pandas>=2.0
python-frontmatter>=1.1
